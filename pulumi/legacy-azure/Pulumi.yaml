name: legacy-azure
description: Importing our old Azure infrastructure before the creds run out
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
